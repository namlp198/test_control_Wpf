<UserControl x:Class="test_control_WPF.Gyro3DControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="300"
             Height="300">
    <Grid>
        <Viewport3D x:Name="viewport"
                    ClipToBounds="True"
                    IsHitTestVisible="True"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
            <Viewport3D.Camera>
                <PerspectiveCamera Position="3,3,5"
                                   LookDirection="-3,-3,-5"
                                   UpDirection="0,1,0"
                                   FieldOfView="35" />
            </Viewport3D.Camera>
            <Viewport3D.Children>
                <!-- các đối tượng 3D sẽ thêm từ code -->
            </Viewport3D.Children>
        </Viewport3D>
    </Grid>

</UserControl>
